[pytest]
# Default command line options (always generate allure report data)
addopts = -v --alluredir=allure-results

# Logging configuration
log_cli = true
log_cli_level = INFO
log_cli_format = %(asctime)s [%(levelname)s] %(message)s
log_cli_date_format = %Y-%m-%d %H:%M:%S